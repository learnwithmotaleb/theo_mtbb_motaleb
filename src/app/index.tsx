// Intentionally empty.
// Navigation is handled by AnimatedSplash in _layout.tsx
// which redirects to /(auth)/login after the splash animation.
import { View } from 'react-native';

export default function Index() {
    return <View />;
}